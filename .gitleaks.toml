# Mensa Italia — config gitleaks
# Estende le regole di default e allowlista i casi noti non-segreti.

title = "Mensa Italia gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "PUBLIC client-side keys + Flutter/web legacy paths"
regexes = [
  # Chiave Google Maps JS della vecchia web app: PUBBLICA by design (usciva nel
  # bundle JS). Protezione = HTTP Referrer restrictions su Google Cloud Console.
  # `webApp/` non esiste piu' in HEAD, ma la chiave resta nella storia del repo:
  # l'allowlist serve ancora a chi scansiona la history, non il working tree.
  '''PUBLIC_GOOGLE_MAPS_API_KEY''',
  '''AIzaSyB2aoF70O1oDQv0BQ3SG6WFQaayIEVEMPE''',
]
paths = [
  # Config client Firebase per Android, generato dalla console.
  #
  # `current_key` e' una chiave client, pubblica per design: viaggia dentro
  # l'APK ed e' estraibile da chiunque scarichi l'app. La protezione vera sono
  # le restrizioni per package name + SHA-1 sulla Google Cloud Console, non la
  # segretezza del valore. Stessa natura della chiave dentro
  # `GoogleService-Info.plist`, committato qui accanto dal primo giorno — con
  # la differenza che quella non fa scattare la regola `gcp-api-key` e questa
  # si', quindi va detto a mano invece di passare per caso.
  #
  # Allowlist per percorso e non per valore: cosi' regge anche a una rotazione
  # della chiave. Il file lo genera Firebase e contiene solo config client; se
  # un giorno ci finisse dentro altro, questa riga andrebbe ristretta.
  '''^androidApp/google-services\.json$''',

  # Flutter legacy: tutto sotto lib/, ios/Runner/, android/app/ era la vecchia
  # app Flutter. Contiene Firebase/Maps client keys che sono pubbliche per
  # design. Le chiavi vanno comunque ruotate (compromesse da > 1 anno) ma
  # non ha senso bloccare la pipeline su file storici non più presenti in HEAD.
  '''^lib/.*\.dart$''',
  '''^lib/firebase_options\.dart$''',
  '''^ios/Runner/.*\.swift$''',
  '''^android/app/google-services\.json$''',
  '''^android/app/src/main/AndroidManifest\.xml$''',
]
